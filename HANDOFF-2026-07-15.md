# Handoff — atlas-world-svc `release/1.2` (2026-07-15)

> New session: memory auto-loads `f-002-asset-pipeline-status`, `universal-asset-previewer`,
> `godot-platform-decision`. This doc is the human-readable snapshot + next steps on top of that.

## Where things stand

- **Branch:** `release/1.2` (open release; last promoted to prod = `1.1` on 2026-07-12).
- **Worktree:** `/Users/pasitnusso/workspace/repos/atlas-world-svc/.claude/worktrees/_release`
  (edit here — the `main` checkout blocks Edit/Write via the ps-release-workflow guard).
- **HEAD:** `89f894c`. Recent, all committed:
  - `89f894c` fix(storybook): surface weapon/zone vfx in sidebar nav
  - `2191257` feat(assets): fill projectile/zone keys with CC0 3D models
  - `7c53e86` feat(storybook): universal asset-preview architecture (Phase 0+1)
  - `ddec451` fix(client): bring camera in so facing is visible
  - `3246653` feat(assets): storybook nav + animation preview + soundboard
  - `eaa791b` feat(client): positional SFX PoC

### ⚠️ Uncommitted working-tree state (decide first thing)
```
 M game-client/project.godot                          # modified — inspect before committing/reverting
?? game-client/assets/icons/icon-health-potion.svg.import
?? game-client/assets/icons/icon-mana-potion.svg.import
?? game-client/src/World/FacingVerify.cs.uid
```
The `.import`/`.uid` files are Godot-generated sidecars; the `project.godot` diff is likely an
autoload/editor change. Review `git diff game-client/project.godot` and either commit intentionally
or `git checkout` it — don't leave it dangling into a promote.

## What was accomplished this session (asset pipeline = F-002)

Complete asset pipeline on `release/1.2`, all three axes working + previewable:

- **Spine:** codegen emits `colyseus-server/generated/asset-keys.json` (19 keys) →
  JSON `game-client/assets/manifest.json` → Godot `AssetRegistry.cs` three-tier resolve
  (bespoke→seed→capsule, never throws) → CI drift-gate `scripts/check_asset_manifest.mjs`.
- **Seed art (CC0):** 8 Kenney "Mini Characters" (player/npc/6 mobs), 1 file each, distinct.
- **Animation PoC** (`AnimationController.cs`) — idle/walk/sprint/attack/death from synced state. `ATLAS_VERIFY_ANIM=1` → 9/9.
- **SFX PoC** (`AudioRegistry.cs` + `audio-manifest.json`) — positional, fires on state edges. Kenney RPG Audio (51 ogg).
- **VFX fill** — 11 `projectile:*`/`zone:*` keys → real `model3d` glTF (guard A forbids 2D for codegen keys). Gate 0/0.
- **Web storybook** `tools/asset-storybook/index.html` — model-viewer gallery, sidebar nav
  (Characters 8 / VFX 11 / Icons 2 / SFX 51), anim dropdown, soundboard, Coverage panel.
  Serve: `python3 -m http.server 8099` from worktree root → `/tools/asset-storybook/index.html`.

## Known limitations / follow-ups (all non-blocking, gate stays green)

1. **VFX file reuse (user chose LEAVE):** only 3 CC0 weapon meshes exist, so `spear.glb` serves
   spear/magicSpear/physicSpear and `melee.glb` serves melee/smallMeelee/largeMeelee. Zones all distinct.
   Later polish: per-key tint+scale on shared mesh, or pull more CC0 weapons.
2. **Mobs are humanoid, not monsters:** all 8 are Kenney people (no CC0 monster pack). Art-swap later
   (Quaternius creatures) — pipeline is fine, purely thematic.
3. **`ATLAS_VERIFY_MANIFEST=1` probe is broken (pre-existing):** asserts `Version==1` but manifest is v2
   → fails on baseline. Quick fix: update the probe to v2.
4. **Character scale:** Kenney "Mini" render ~0.67–0.79u vs ~1.8u target — needs a scale pass.
5. **Zones verified indirectly in-game** (spawn via `debug_spawn_trap`, not passive sim); weapon in-flight
   scale/orientation may need tuning (spear import-scaled 0.15×).
6. **`sfx:death` is a stand-in** (metalLatch) — grab Kenney Impact Sounds for a real sample.
7. **No combined in-game play-test** (see anim + hear SFX together in a window) done yet.

## Coordination — TWO sessions share `release/1.2`

- **This session** owns the vfx-seeding lane (done) + the nav fix.
- **Parallel session `c5cdaa4f`** owns the **universal-previewer roadmap** (memory: `universal-asset-previewer`).
  Its Phases 2–6 remain (SpriteFrames/UI kits, fonts/json, baked-preview+staleness gate, three.js family,
  CC0 seeding). **Its Phase 6 no longer needs projectiles/zones — this session filled them.**
  The storybook `index.html` is co-owned — stay surgical there.

## Suggested next actions (pick one to start the new session)

- **A — Promote `release/1.2` to prod.** Coherent, verified, banked. Use ps-release-workflow promote
  (`--allow-missing-gate2`, needs explicit auth; atlas has no Gate 2). Resolve the uncommitted
  `project.godot` first.
- **B — Quick wins:** fix the `ATLAS_VERIFY_MANIFEST` v2 probe + character scale pass.
- **C — Continue previewer** phases with the parallel session (or let it run).
- **D — In-game play-test** window: models + animation + SFX together.

## Cost note
This session ran to ~$1925 (huge Opus context = every main-thread call is expensive). The working pattern
that kept it down: **offload browser/ingest/build/verify to fresh Sonnet subagents**, keep the Opus main
thread for decisions only. Do the same next session.

## Run commands (reference)
- Server tests: `cd colyseus-server && npm test`
- Asset drift-gate: `node scripts/check_asset_manifest.mjs` (add `--require-complete` for hard-fail on unmapped)
- Godot headless verify: `arch -arm64 env DOTNET_ROOT=$HOME/.dotnet-arm64 PATH=$HOME/.dotnet-arm64:$PATH ATLAS_VERIFY_ENTITYVIEW=1 ~/Applications/Godot_mono.app/Contents/MacOS/Godot --path game-client --headless --quit-after 60`
- Storybook: `python3 -m http.server 8099` (from worktree root) → `http://localhost:8099/tools/asset-storybook/index.html`
