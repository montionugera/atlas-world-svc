# Registry reconciliation + content→runtime binding spine (event contract, kind taxonomy, CI-verified client resolver) — plan placeholder

Empty until promoted to F-NNN and filled via `/superpowers:writing-plans`.
