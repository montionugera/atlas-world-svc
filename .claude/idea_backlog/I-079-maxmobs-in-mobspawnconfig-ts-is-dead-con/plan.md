# maxMobs in mobSpawnConfig.ts is dead config: MobLifeCycleManager reads only area.count (:71) and never maxMobs, so the documented per-room cap of 8 does nothing and the room already runs 17 mobs after F-031 — either enforce it or delete it, but stop shipping a number that lies — plan placeholder

Empty until promoted to F-NNN and filled via `/superpowers:writing-plans`.
