
## Filed 2026-08-09 during F-040 (town plan) — off-goal, do not chase
- Three cluster-1 roads name a non-place in `from`/`to` (one road id, two zone ids) — `content/maps/cluster1-geography.json`.
- `flat-crossing` (-4.6%) and `terrace-track` (-1.3%) are LONGER than their declared `roadKm`, contradicting the file's own "0-15% short" claim.
- Mob radii are 3-9, not 3-5 (doubleAttacker 8, thorncrownDrake 9). The T3 cart-road floor of 12 clears radius <=5 only; the two largest mobs cannot enter any town authored to it. Decide: widen the floor to ~20, or rule the big mobs out of towns (design S10 q3).
- design 2026-08-09-town-plan-view-design.md S3 says "roughly nine player-diameters" where 12/2.6 = 4.6 diameters; it is nine player RADII. The 12 floor is unaffected.
- Renderer PNGs are byte-nondeterministic (ImageMagick embeds a creation timestamp); decoded pixels ARE identical. Consequence: a town-plan PNG can never be used as a byte-diff drift gate. Compare decoded RGB if a drift gate is ever wanted.

## Filed 2026-08-23 during F-048 seam 7 (the world gates) — off-goal, do not chase
- **The biome classifier and the landform lexicon disagree about how dry a desert is.** c05/r06 is labelled 100% `desert` / `terrainKind: "sand-sea"` and all 640 of its cells are `rock: clastic`, but its own moisture field puts them at precip decile 3 (151 cells), 4 (342) and 5 (146) — one cell at 2, none at 0–1 — while EVERY desert row in c05's kit requires `precipDecileMax: 1`. The region is therefore uninhabitable by its own kit: 0 of 58 types have a single satisfying cell, so it is a SURVEYED region with zero instances, zero settlements and zero anchors, and it is one of the five recorded G-POI floor shortfalls. This is a THRESHOLD DISAGREEMENT, not a placement bug — Task 8's instancing could not have prevented it. Whoever owns biome thresholds should decide which side moves: the classifier's aridity cut, or the lexicon's `precipDecileMax`. A reseed or Plan E redraw can mint more of these, and each new one reds `generate-world.test.mjs`'s golden with no available fix.
- The widest region ring is **194 of a hard cap of 200**, duplicated in `content/schemas/fabric-file.schema.json` as `maxItems: 200`. 3% headroom. The gate prints it on every run, which is the right mitigation, but **Plan E's redraw should treat 194 as a number it is about to move** — a redraw that adds seven vertices to that ring hard-reds at two venues at once.
  **Re-verified 2026-08-29 (F-051 completion Task 6, backlog triage), after the redraw: still 194/200, byte-identical.** `node scripts/check_content.mjs --require-complete` at `786a709` prints `G-POLY: ... widest region 194/200` — Plan E's redraw moved edges, nodes and pins but never touched region-ring vertex counts, so the predicted collision did not happen. The headroom and the residual risk are unchanged; left filed here, not promoted.
