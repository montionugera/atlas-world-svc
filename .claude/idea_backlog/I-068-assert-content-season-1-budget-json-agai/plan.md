# Assert content/season-1-budget.json against DR-003 section 3 — nothing checks the two agree, which is the exact drift the JSON was created to prevent and which DR-003 itself declares impossible; a ~15-line test on {id,target} pairs closes it (string equality on source will not work, 3 of 8 cells are normalized prose) — plan placeholder

Empty until promoted to F-NNN and filled via `/superpowers:writing-plans`.
