---
title: "No nightly CI job runs the load harness, so the capacity table will silently rot"
id: I-073
status: idea
---

# No nightly CI job runs the load harness, so the capacity table will silently rot

## Problem

(what hurts; concrete examples)

## Why now

(opportunity; deadline; constraint)

## Sketch

(rough shape; not a design yet)
