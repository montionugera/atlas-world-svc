# Seamless large-world scaling: AOI (StateView) + shard grid + border ghosts + authority handoff + cross-shard combat — plan placeholder

Empty until promoted to F-NNN and filled via `/superpowers:writing-plans`.
