---
title: "Character-content binding hardening (deferred F-007 phases): NPC config home (src/config/npcs) + generated character-bridge.json codegen + triple validation + enum↔number-band consistency check + wire --require-complete into CI"
id: I-014
status: idea
---

# Character-content binding hardening (deferred F-007 phases): NPC config home (src/config/npcs) + generated character-bridge.json codegen + triple validation + enum↔number-band consistency check + wire --require-complete into CI

## Problem

(what hurts; concrete examples)

## Why now

(opportunity; deadline; constraint)

## Sketch

(rough shape; not a design yet)
