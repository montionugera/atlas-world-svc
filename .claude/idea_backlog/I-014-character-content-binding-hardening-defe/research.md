# Character-content binding hardening (deferred F-007 phases): NPC config home (src/config/npcs) + generated character-bridge.json codegen + triple validation + enum↔number-band consistency check + wire --require-complete into CI — research notes

(prior art, related issues, open questions)
