---
title: "World fill Plan E: redraw and prose — THE REDRAW commit (unfreeze deepest-first, one redraw commit with revert, refreeze root-first), the 13 continent sheets, G-CITE + the citation sweep, survey as a first-class field, the canon-leg pre-flight, world-digest, Z2 against the fabric, and the zone allocation table solved before writing a word"
id: I-103
status: captured
---

# World fill Plan E: redraw and prose

## Approved design and plan already exist — do not re-derive

This idea is the **fifth and final execution slice** of an already-approved design.

- **Design (approved):** `docs/superpowers/specs/2026-08-16-world-fill-generated-land-bound-places-design.md`
- **Plan (written; per-phase seam review happens during execution):**
  `docs/superpowers/plans/2026-08-16-world-fill-e-redraw-and-prose.md` (13 tasks)
- **Running handover state — read first:** `docs/superpowers/plans/world-fill-STATE.md`
  (§1–§27: measured baselines, Plan D's shipped divergences, the filed hazards, and the
  obligations Plan E owns — see below).
- Sibling slices: A (F-046, shipped), B (F-047, shipped), C (F-048, shipped),
  D (**F-049, shipped on 1.8**).

## Problem

The generated world exists and is joined to authored meaning, but the committed chart,
spine freezes, sheets and prose still describe the retired cluster-1 frame:

- the spine trunk is still the old map — trunk sea:land reads 24.63 : 1 against the
  fabric's 1.50 : 1, and Gate 2 carries ~172 legacy geography orphans until movement 2;
- no continent-level zoom sheets exist (the 13-sheet tier nobody built);
- citations in prose point at authorities that moved or died;
- `survey` lives in prose conventions, not as a first-class field;
- zone prose records (40) were never re-homed onto the new regions.

## Obligations Plan E inherited from Plans A–D (from STATE — do not re-derive)

- Clear the bounded Gate 2 red window (~172 named geography orphans, §26).
- Re-home the alias-sweep data (116 bestiary rows, 10 story regions, 6 art keys,
  10 zone files, 1 town plan) onto new region ids.
- Decide road ink over reported ground (spec §6.4 rule 2 was unsatisfiable as written);
  author or record-why-not the network/betweenness/adjacency relations (§23 follow-up).
- Re-derive the rename+geoId alias fixture lost in Task 11's cutover (§26 erratum 8).
- Judge the 26 named pin deviations if any prose claims need revoicing.
- The redraw commit may not contain a hand edit; R12 re-baseline order runs against a
  lock that has already moved once by design.
