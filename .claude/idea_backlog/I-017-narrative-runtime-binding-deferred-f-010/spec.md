---
title: "Narrative runtime binding (deferred F-010): @atlas/contracts STORY catalog (goja-safe require, zod, validateCatalogIntegrity) + QuestDef narrative fields (title/description/offerText/giver/region/faction — backfill ALL 3 quests or contracts import crashes Nakama) + reconcile q_boar_5 'boar'->real mob id + ZONE_ENTERED emitter scaffold (real zoneIds need maps-loader I-015) + accept_quest prereq-gating only (giver-gating needs in-world NPC trigger, blocker) + C# mirror regen + drift check. Needs contracts/nakama/colyseus verify + goja bundle smoke"
id: I-017
status: idea
---

# Narrative runtime binding (deferred F-010): @atlas/contracts STORY catalog (goja-safe require, zod, validateCatalogIntegrity) + QuestDef narrative fields (title/description/offerText/giver/region/faction — backfill ALL 3 quests or contracts import crashes Nakama) + reconcile q_boar_5 'boar'->real mob id + ZONE_ENTERED emitter scaffold (real zoneIds need maps-loader I-015) + accept_quest prereq-gating only (giver-gating needs in-world NPC trigger, blocker) + C# mirror regen + drift check. Needs contracts/nakama/colyseus verify + goja bundle smoke

## Problem

(what hurts; concrete examples)

## Why now

(opportunity; deadline; constraint)

## Sketch

(rough shape; not a design yet)
