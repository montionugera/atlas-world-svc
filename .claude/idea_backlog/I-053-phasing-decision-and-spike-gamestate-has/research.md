# Phasing decision and spike: GameState has zero @filter/filterChildren/StateView, so two clients in one room cannot see different world contents - cheapest to build now at 28 quests, cannot be retrofitted at 560 — research notes

(prior art, related issues, open questions)
