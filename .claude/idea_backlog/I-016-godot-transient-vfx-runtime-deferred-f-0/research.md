# Godot transient-VFX runtime (deferred F-009): VfxRegistry autoload + single-path Spawn(options) one-shot auto-free (loop forced off) reading catalog kind:vfx directly; bind hit->fx:explosion / death->fx:fireball / heal->fx:heal_sparkle at existing SFX edge sites (AnimationController/EntityView), zero SFX regression; VfxVerify probe asserting derived cols + first/last frame rect (not just count). Needs local Godot to verify. Also: real skill/cast RoomEventType broadcast for cast-glow VFX (cross-domain server work) — research notes

(prior art, related issues, open questions)
