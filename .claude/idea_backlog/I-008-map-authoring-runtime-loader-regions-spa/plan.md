# Map authoring + runtime loader (regions, spawn points, static zone hazards, mob spawn tables) with legacy fallback — plan placeholder

Empty until promoted to F-NNN and filled via `/superpowers:writing-plans`.
