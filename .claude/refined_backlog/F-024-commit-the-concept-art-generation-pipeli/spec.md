---
title: "Commit the concept-art generation pipeline into the repo (Z-Image Turbo img2img + silhouette recipe + prompt laws + batch matrix scripts) — currently scratchpad-only and dies with the session"
id: F-024
from_idea: I-031
status: refined
---

# Commit the concept-art generation pipeline into the repo (Z-Image Turbo img2img + silhouette recipe + prompt laws + batch matrix scripts) — currently scratchpad-only and dies with the session — design

## Goal

(one sentence)

## Architecture

(2-3 sentences)

## Components

(list — each with one responsibility)

## Data flow / state

## Tests / acceptance criteria
