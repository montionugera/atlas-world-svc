# Commit the concept-art generation pipeline into the repo (Z-Image Turbo img2img + silhouette recipe + prompt laws + batch matrix scripts) — currently scratchpad-only and dies with the session Implementation Plan

> Fill via `/superpowers:writing-plans` once the spec is final.
