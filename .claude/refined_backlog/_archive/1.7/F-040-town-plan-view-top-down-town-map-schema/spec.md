---
title: "Town plan view: top-down town map schema (boundary, road spine with widths, building footprints, plazas, landmarks) authored from A1 section 6 themes and bound to the physics scale, plus the footprint to static-collision binder"
id: F-040
from_idea: I-088
status: refined
---

# Town plan view: top-down town map schema (boundary, road spine with widths, building footprints, plazas, landmarks) authored from A1 section 6 themes and bound to the physics scale, plus the footprint to static-collision binder — design

## Goal

(one sentence)

## Architecture

(2-3 sentences)

## Components

(list — each with one responsibility)

## Data flow / state

## Tests / acceptance criteria
