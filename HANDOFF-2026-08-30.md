# HANDOFF — 2026-08-30 · Millcross walled town + concept-art pipeline (feat/F-039)

**Read this first if you are resuming:** the work happens in the claimed worktree
`.claude/worktrees/F-039-town-concept-art-the-block-in-cannot-exp`, branch `feat/F-039`.
Everything below is committed there. The main checkout is NOT where this work lives.

---

## Goal (verbatim — do not widen, do not narrow)

> Owner direction, 2026-08-29/30: remove the tent quarter from Millcross world truth; make
> Millcross a **walled town with quest/shop venues** ("we do want wall and more building so we
> have places for quest and shop" — consumable shop, weapon shop, ingredient shop, inn, food
> shop, adventurer guild); produce the **concept image derived from the town map** at
> **long-term high quality** (FLUX.1-dev path, DR-002 licence accepted), reviewed by the
> Town Canon & Plausibility Reviewer until her PASS, then wire results into the storybook,
> finish Phase 5 bookkeeping, and ship via merge (path 1: cluster storybook updates on merge).

## Where the state lives (all paths relative to the worktree root)

| Artifact | Path |
| --- | --- |
| Town plan (walled, 25 footprints) | `content/towns/town-millcross.json` |
| Town criteria (34 rules, reviewer-owned) | `content/world/town-criteria.json` |
| Consistency gate | `scripts/tests/town-criteria.test.mjs` (8 tests, mutation-verified) |
| Reviewer agent definition | `.claude/agents/town-canon-reviewer.md` |
| Reviewer verdict sheets | `docs/worldbuilding/reviews/2026-08-2*.md` (4 sheets) |
| Negative result (segment ladder) | `docs/worldbuilding/ABP-segment-control.md` |
| Dev/anchor measured recipe | `docs/worldbuilding/ABP-flux-dev-and-anchor.md` |
| Brief (walled, medium-clause ready) | `tools/art-forge/briefs/A1-ART-02.json` |
| Run ledger (provenance per roll) | `tools/art-forge/runs/A1-ART-02.json` + `_index.json` |
| Rendered map | `docs/worldbuilding/A3-town-millcross-plan.png` (1800×1756) |
| Renders (gitignored) | `tools/art-forge/out/env/*.png` |
| Board card | herdr-board card #8 (atlas-world-svc, comments 41–44) |

## Key commits on feat/F-039 (newest last)

`49c3b28` segment-ladder negative result → `5f47fb3` freehand `--control none` →
`eccbd05` brief: solid town → `928a8af` merge release/1.8 → `6894e97` **tent quarter removed**
→ `4a0ee15` review reconciliation → `7d3ee96` **reviewer agent** → `dd39861` consistency gate →
`a8aec88` ledger provenance → `09a4dcd` **criteria v1** → `039f26a` lint merges criteria vocab →
`579836d` **walled town amendment** → `d210d18`/`25ba7e1` reviewer prompt additions →
`424c8f5` **dev model + anchor pass** → `3944eba` medium clause → `f6b81c9` ledger.

## Canon state (all gates green at last run)

- Millcross: **walled** west-bank core (timber-and-earth wall, 6u thick), 3 gate towers
  (2 flank the west high street, 1 the ford approach), **18 venues** incl. inn + guild hall
  (2-storey) + provisioner/herbalist/weapon-smithy/bakehouse/general-store, mill-house still the
  tallest mass at the race. 25 footprints total. Composition re-derived: built 31 / meadow 60 /
  river 9. Schema kinds: +wall/gate/inn/guild (tent REMOVED — owner decision).
- A1 §6 founding sentence amended; A3 traceability rows updated (38 ids: 4 CANON-ID · 9
  CANON-THING · 9 INVENTED · 16 OWNER-DECISION); anchor re-derived `[98.2, 152.6]` from the
  resolved join.
- Gates: scripts 1371/1371 · content 0 failures · art-forge 216/0 · town-statics 7/7 ·
  world-digest + geometry-lock clean (re-baseline after ANY change to `content/spine/nodes/`).

## The review loop (4 rounds so far)

Reviewer = dispatch `general` subagent with prompt "read
`.claude/agents/town-canon-reviewer.md` first — you ARE that role; write only
`docs/worldbuilding/reviews/**`". Sheets: freehand → walled depth → ford → dev.

| Round | Verdict | What it taught |
| --- | --- | --- |
| freehand (3 seeds) | all fail | "a camp, not a town" — nothing bound to the map |
| walled depth 0.30 (3) | all fail | wall/gate/venues land; contamination + material drift |
| ford-asserted depth 0.30 (3) | all fail | **escalation: cfg-1 sampler-attention failure** — bridge/pantile persisted across briefs |
| dev 0.30 (3) | all fail, register salvageable | fix = medium clause + 0.40 + material nouns |
| **dev 0.40 + medium (3)** | **strongest yet — seed12345 s0.40** | bridge recurrence is the standing defect |

## IN FLIGHT — Forge tab UI redesign (the exact task that was interrupted)

Owner feedback: the Forge tab renders ~130 unreadable cells (one per ledger entry — every run
appends a `blockin` entry AND a `render` entry) and should look like a GitHub Actions run
graph (screenshot shared: grouped cards, clean flow). Diagnosis complete, NO code written yet:

- `tools/asset-storybook/js/forge/forge.mjs` — `buildPipelineRow`/`wireRow` render one cell per
  `attempts[]` entry (read lines ~280–340).
- Redesign spec (agreed with owner):
  1. Keep only `type === "render"` entries as cards; drop `blockin` cells (supporting stage).
  2. Group cards by `briefHash` (= recipe version), newest batch first, batch header with count.
  3. Each card = **PNG thumbnail** (`out` path → URL `../../tools/art-forge/<out>` from the page)
     + label `seed · control · strength · -dev- if model dev` + stale/gate badge.
  4. Grid/gallery layout in the Forge section CSS (`index.html`).
- Thumbnail URLs work on the local server (worktree root served; `out/` is gitignored but local).

## Next steps, in order

1. **Forge tab redesign** per the spec above (the interrupted task).
2. **Reviewer dispatch** on the three s0.40 cells (`out/env/A1-ART-02-dev-seed{12345,42424,10001}-s0.40.png`,
   ledger: control=depth strength=0.4, model dev) — ask for minimal positive-form change set; expect
   the bridge to be the remaining defect.
3. **Iterate rolls** (same settings, fresh seeds) until PASS. If the bridge persists across 3+ cells,
   escalate like the pantile round: add a positive clause ("the road fords the river at the shallow
   gravel bed; carts cross IN the water") or consider the anchor-pass variant once style register settles.
4. **Storybook wiring** (reviewer flagged): renders absent from `world-index.json` — wire the winning
   render into the art review surface per the 2026-08-15 owner rule.
5. **Workflow doc**: `docs/worldbuilding/ABP-town-concept-workflow.md` — the stage machine
   (brief-check → map-derive → generate ≤5 cells → reviewer → owner verdict → record/stop), now
   including the dev/anchor recipe and medium clause.
6. **Phase 5**: F-039 plan Tasks 4–8 supersede note; six-town roll-out pointer; board card close.
7. **Ship**: `psrw ship` → release promote → cluster storybook rebuilds automatically (path 1).

## Commands (from the worktree root)

```bash
# gates
node scripts/check_content.mjs                          # content gate (exit 0 required)
npm test --prefix scripts                               # 1371 tests incl. town + criteria gates
(cd tools/art-forge && node --test tests/*.test.mjs)    # art-forge suite (216)
(cd colyseus-server && npm test -- tests/town-statics.test.ts)
node scripts/check_world_digest.mjs --check             # after ANY content/spine edit:
node scripts/check_spine_emit.mjs --write               #   regenerate derived
node scripts/check_world_digest.mjs --write             #   re-baseline digest
node scripts/check_geometry_lock.mjs --write            #   re-baseline geometry (SLOW ~3-5 min)
node tools/art-forge/ledger-index.mjs                   # rebuild runs/_index.json (Forge tab needs it)

# roll a dev+anchor cell (depth 0.40, walled brief)
(cd tools/art-forge && node generate/env.mjs --brief A1-ART-02 --seed <N> \
  --model dev --control depth --strength 0.40 --anchor)

# review surface
nohup python3 -m http.server 6007 --bind 127.0.0.1 &    # then open
# http://127.0.0.1:6007/tools/asset-storybook/index.html  → Forge tab
```

## Open decisions / known defects

- **Bridge recurrence** in renders (canon: exactly one cart crossing = the ford, no bridge).
  Round-4 cells still show one. Reviewer change set pending on the s0.40 cells.
- **Pantile/fachwerk drift** — reduced by the medium clause but not eliminated; A8 instability
  is dev's measured trait. Escalation ruling: cfg A/B was superseded by the dev pivot.
- **Anchor style hijack** — the img2img pass still went flat-vector despite grain; parked
  (dev BASE renders are the current path). Denoise window may differ for our depth map.
- **Reviewer's open flag**: renders not in `world-index.json` (owner rule) — step 4.
- **Quest gate contradiction CLOSED by the wall** (quest-208 "meet the road at the gate" now
  lands on the real west gate) — kept one release in criteria `knownOpenItems` for traceability.

## Session context

- Reviewer agent dispatches via `general` subagent with her definition file inlined in the prompt
  (harness-agnostic fallback, per audit F-3).
- Board card #8 carries comments 41–44 with the full narrative.
- The GPU host is up via the SSH tunnel (port 8188 → 200) — re-check with
  `curl -s http://127.0.0.1:8188/system_stats` before any roll.
- Herdr-board card #8 is the coordination surface; local storybook server dies on reboot
  (restart command above).
