{
  "version": 1,
  "about": "Budgets for the layers that live OUTSIDE content/spine/ and are therefore invisible to G-LOAD-BUDGET. Every gate that reads this file PRINTS its measurement on every run — the G-LOAD-BUDGET / G-COMP-REPORT discipline — so drift is visible long before it is a failure. Plan B's gSpineWorld owns the `landforms` and `sheets` sections (G-LANDFORM, G-SHEET-BUDGET); Plan C adds `fabric`, `civil` and `loop` and owns G-WORLD-BUDGET, including this file's existence check.",
  "reportLineGrammar": "`world-budget: <family> <n> files, <n> bytes (budget <n>, <n>)` is the grammar for families measured as FILES ON DISK — `sheets` today, `fabric` and `civil` when Plan C lands. A family whose budget is not files and bytes prints its own units after the same prefix: `landforms` reports `<n> types, <n> instances (budget <lo>-<hi> types, <n> instances)`, because it has no files of its own to count. Any parser of these lines must key on the family name, not on a single fixed shape.",
  "cellKm": 0.5,
  "cellKmWhy": "PINNED CONSTANT, same discipline as KM_TO_U = 100 and SPINE_CELL_KM = 0.05. 0.5 km over the 400 x 400 km frame is an 800 x 800 grid = 640,000 cells ~= 14.7 MB resident. Changing it changes every landform predicate's meaning.",
  "landforms": {
    "maxInstances": 2400,
    "maxNamed": 500,
    "minTypes": 100,
    "maxTypes": 200,
    "typeCoverageFloor": 100,
    "dungeonCapableTypes": 23
  },
  "landformsWhy": {
    "typeCoverageFloor": "100 of the catalogue's types must actually appear in content/world/fabric/ — a QUOTA, not a target, and the number is real: G-LANDFORM enforces exactly it. It arms only once the fabric holds at least one instance (an empty content/world/fabric/ directory is not a fabric layer), and it WARNS on the development gates while FAILING under --require-complete, which is Gate 2 at promote. An incrementally built fabric therefore degrades and reports; a release with a half-built one does not ship. Raising this number without adding a line here is a budget failure, not a fix.",
    "absentBecause": "A lexicon row's absentBecause is a DECLARATION, not a second floor. Declared-and-yet-placed is a hard failure (a stated reason contradicted by a real instance). Unplaced-with-null is a WARNING only: as a failure it demanded all 170 rows be placed or excused, which is a strict superset of typeCoverageFloor and made the number above unreachable — 100 types placed still failed 70 times."
  },
  "sheets": {
    "maxSheets": 18,
    "maxSvgBytes": 524288,
    "maxRasterSeconds": 2,
    "maxPatternRectAreaRatio": 1.5,
    "maxPatternRectClipRatio": 3,
    "rasterWidthPx": 2000,
    "thumbWidthPx": 512,
    "maxThumbBytes": 393216,
    "minThumbInkFraction": 0.02,
    "minThumbInkRowFraction": 0.5,
    "minThumbDistinctColours": 64
  },
  "sheetsWhy": {
    "maxSheets": "The roster, enumerated: 1 atlas + 1 basin + 13 continent (Plan E) + 1 overlay + 1 fabric (Plan C) + 1 synthetic (Plan B) = 18. The design's \"<= 16\" counted the shipped chart only (1 atlas + 13 continents + 1 basin + 1 overlay) and omitted the review sheets the owner's every-artifact-observable rule requires. Raising this cap without adding a line here is a budget failure, not a fix.",
    "maxSvgBytes": "512 KB, and it STAYS 512 KB: the ceiling above it is not ours to raise. librsvg's libxml2 refuses any single attribute over 10,000,000 bytes (XML_MAX_TEXT_LENGTH, no XML_PARSE_HUGE), measured OK at a 1360 px square frame and FAILING at 1370 px, so a baked underlay's data URI has a hard parser cliff 20x above this budget regardless of what the budget says. Measured 2026-08-20 at the atlas sheet's real 1400x1400 frame with 160 irregular regions: underlay 78,178 B / 0.25 s at 2000 px, whole canary sheet ~248 KB / ~0.79 s — 1.6-2.1x inside this cap. THAT MEASUREMENT IS NOT ROBUST TO COLOUR VARIATION. It rests on the frame holding ~9 distinct colours and on strict tile periodicity, not on geometry: nearly quadrupling the region count to 600 costs only +27%, but a per-region TINT pass drops headroom from 3.3x to 1.33x, and tint plus dither blows 512 KB under every encoding tried, including off-the-shelf zlib -9. Anyone proposing per-region colour variation is proposing to re-decide this number, and must re-measure before doing so. Raising this cap without adding a line here is a budget failure, not a fix.",
    "maxPatternRectAreaRatio": "The STRUCTURAL half of the sheet budget, added by Plan B Task 12 because the byte half was not guarding what it looked like it guarded. The basin sheet measured 47,020 B — 11x inside maxSvgBytes — and 11.31 s at rasterWidthPx, 5.7x OVER maxRasterSeconds. Bytes and raster cost are not correlated: librsvg's pattern cost scales with the PIXEL AREA a fill covers, and the sheet drew twelve <rect>s each covering the whole 990x1254 map frame, then clipped every one of them down to a single small zone. 47 KB of text, twelve full-frame tilings. This number is the total area of pattern-filled <rect>s as a multiple of the sheet's own canvas, which is readable straight out of the committed text and therefore checkable by a gate — maxRasterSeconds is not, because a gate must not shell out to rsvg-convert and CI has no librsvg. MEASURED 2026-08-22: defective basin sheet 6.61, fixed basin sheet 0.40, atlas 0.00 (its fills are on <path>s, which cost the shape they actually ink), canary 0.002 (a baked <image> underlay plus 40x24 legend swatches). 1.5 admits a sheet that legitimately pattern-fills its whole canvas and half of it again; it rejects the 6.61 it was calibrated on by 4.4x. Raising this cap without adding a line here is a budget failure, not a fix.",
    "maxPatternRectClipRatio": "The DIRECT half of the same rule, added by the F-047 seam-4 fix pass after the two reviewers disagreed about maxPatternRectAreaRatio and both were right. That cap is an AGGREGATE over a whole sheet: verified to fire at 6.61 on the twelve-zone defect, and verified to PASS the same defect at two zones (0.397 + 2 x 0.49 = 1.38 < 1.5). It is a total-cost budget and nothing else measures total cost, so it stays. This number measures the MISTAKE instead \u2014 one fill at a time, as a multiple of its OWN clip path's bounding box \u2014 so it fires on the FIRST regressed zone rather than waiting for a quorum. MEASURED 2026-08-22 on the committed sheets: worst 1.058 (cluster1 clip-hollowmarch), then 1.053, 1.048, 1.043; the atlas and canary legend swatches carry no clip-path and are exempt, because an unclipped fill is not hiding anything. A single full-frame regression on cluster1\u2019s LARGEST zone scores 24x and on a small one several hundred, so 3 sits 2.8x over the real worst and at least 8x under any regression. Fills whose clip box is under 32 px on a side are skipped: there the emitter\u2019s own 2 px edge slack dominates the ratio and the absolute cost is nil, next to the 1,241,460 px\u00b2 rects this rule exists for. Raising this cap without adding a line here is a budget failure, not a fix.",
    "maxRasterSeconds": "Read by Plan B Task 10's synthetic-sheet raster test, not by a spine gate — a gate must not shell out to rsvg-convert.",
    "rasterWidthPx": "The width maxRasterSeconds is measured at. A raster budget without its width is not a budget. It is NOT the width anything is committed at — see thumbWidthPx.",
    "thumbWidthPx": "512 px, and it is the width of the ONLY raster that is ever committed. `render-sheet.mjs --png` writes at this width; `--png-width 2000` produces the ship raster on demand and that file is never added. The two numbers are separate because they answer different questions: rasterWidthPx is what the 2 s raster budget is measured at, thumbWidthPx is what LFS pays for. 512 px is the smallest width at which F-044's finding still holds — the sheet's sub-pixel ink survives the downscale and the storybook card stays legible; the vector itself vanishes at card scale, which is why a raster is committed at all.",
    "maxThumbBytes": "384 KB per committed sheet PNG, enforced by tools/asset-storybook/tests/maps-index.test.mjs (Gate 1 AND CI), not by a spine gate — a gate must not shell out to rsvg-convert. The number is the reason the rule exists: .gitattributes:29 puts game-client/assets/**/*.png in LFS with no cross-version dedup, and the target-density canary shipped at 2,534,694 B before this budget, for a sheet no player ever sees. MEASURED at thumbWidthPx = 512 on 2026-08-21: atlas 50,523 B, cluster1 152,208 B, synthetic canary 312,224 B. The cap is set from the canary, which is deliberately the densest sheet the roster will ever hold (1,740 glyph instances, 340 labels, a full-frame baked underlay) — 1.26x headroom over it, and the pre-rule 2,534,694 B canary is still 6.4x over, so the rule fires on exactly what it was calibrated to catch. The plan predicted \"near 100 KB each\" and a 262,144 cap; that held for the two real sheets and was refuted by the canary, which the plan was written before. Raising this cap without adding a line here is a budget failure, not a fix.",
    "minThumbInkFraction": "THE FLOOR THAT DID NOT EXIST. Every rule on a committed thumb bound it from ABOVE \u2014 maxThumbBytes caps it, thumbWidthPx pins its width \u2014 so a BLANK image passed all of them. Proved at d86f948 by both seam-4 reviewers independently: a 1,899 B blank 512 px PNG substituted for the atlas thumb left the storybook suite 47/0 and check_render_lock, check_asset_manifest and check_content all clean. The one guard that claimed to see it, render-sheet.test.mjs's `size > 10000` on the ship raster, cannot: a blank 2000 px raster measures 14,079-18,074 B depending on the librsvg build, well over its own floor. Compressed size is the wrong variable for the same reason it was the wrong variable for raster cost. This number is the share of PIXELS that differ from the image's own modal colour by more than 8/255 per channel, read by tools/mapforge/lib/png-ink.mjs, which decodes the committed bytes and needs no rendering library \u2014 so it runs in CI, which has none. MEASURED 2026-08-22 on the committed thumbs: atlas 0.0545, cluster1 0.2880, canary 0.3037; a blank page of any size scores exactly 0. 0.02 is 2.7x under the sparsest real sheet and infinitely over a blank one. Lowering this floor without adding a line here is a budget failure, not a fix.",
    "minThumbInkRowFraction": "The second half of the ink floor, because one number cannot see two different ways of being blank. minThumbInkFraction catches the uniform page; this catches the page whose ink is all in one blot \u2014 a corner watermark, a legend block with an empty map beside it. It is the share of SCANLINES carrying at least one non-modal pixel. MEASURED 2026-08-22: atlas 0.893, cluster1 0.914, canary 0.947, blank 0. 0.5 says half the height of a map sheet must have something drawn on it, which is 1.8x under the sparsest real sheet.",
    "minThumbDistinctColours": "The third blank mode: a flat two-colour placeholder that a fraction and a row count both admit. A map sheet is parchment, ink, biome fills, coast echoes and antialiasing \u2014 MEASURED 2026-08-22: atlas 1,282 distinct RGB values, cluster1 2,870, canary 1,573. A blank page has exactly 1. 64 is 20x under the sparsest real sheet and rejects anything that is not actually a drawing."
  }
}
